# Copy to .streamlit/secrets.toml locally (that file is gitignored), or paste into
# Streamlit Community Cloud → Your app → ⋮ → Settings → Secrets.

GROQ_API_KEY = "gsk_replace_me"
# Optional — default is llama-3.3-70b-versatile
# GROQ_MODEL = "llama-3.1-8b-instant"
